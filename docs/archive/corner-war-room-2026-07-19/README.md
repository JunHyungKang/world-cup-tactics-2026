# Corner War Room frozen runner-up package

This directory preserves the text surfaces that were canonical when Corner War
Room scored `97/100` in the exact same-reviewer comparison. The generated PDF,
video, gallery, planning, and storyboard artifacts remain at their original paths
and are byte-bound in `archive-manifest.json`.

The archive is evidence, not the selected submission product. Corner Policy Lab
v5 is the promoted official candidate at `98/100`. Do not copy human-validation,
public-deployment, YouTube, or DAKER claims from this archive; none existed at
freeze time.
