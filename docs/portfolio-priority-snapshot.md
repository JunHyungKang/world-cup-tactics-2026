# Portfolio priority snapshot

Captured: `2026-07-28 KST`

This repository-contained snapshot makes the public verification contract
self-contained. The private multi-project board remains the live portfolio
source of truth and must be refreshed at the start of each serious session.

| Priority | Competition | Current gate |
|---|---|---|
| P0 | World Cup Tactics Web Challenge | Corner Scout Lab is the canonical root product; the reviewed PDF `060376c8…` is submitted, causal recommendation remains REJECT, and the wider empirical campaign remains REVISE. The old `5/4/1` allocation video and the five-signature-only video are superseded and must not be uploaded. Events, Matches, and Players are accepted. The current first screen corrects Portugal's 14-corner sample with a 397-corner tournament prior (`47% / 53%`) and exposes the frozen 160-corner audit (`4.59%` log-loss reduction). Uruguay's five classifiable defensive exposures failed the two-team forecast gate (`P=0.9226 < 0.975`) and remain source-scene evidence only. The manager then compares Portugal/Uruguay signature pairs `7/2 · 1/2 · 4/0 · 1/0 · 1/1`, locks exactly two video-review questions, reveals held-out counts `5/2/0/0/3`, shot counts `2/0/0/0/2`, and counterevidence `261095314`. Zero means an observation gap, not a weakness. The corrected `122/122` unit/contract suite, `17/17` source interactions, `12/12` static release matrix, and `56/56` pre-release matrix pass. Exact-public, gallery, and video evidence must still be regenerated. Next chain: public commit/build → exact-public matrix → new sub-60-second video and independent review. YouTube publication and the final DAKER receipt remain owner-controlled gates. |
| P1 | ROGII – Wellbore Geology Prediction | One bounded GPU hypothesis may run only without consuming the World Cup planning gate |

Policy: keep one P0 execution lane and one bounded P1 exploration lane. This
snapshot is evidence of the captured constraint, not a replacement for live
portfolio coordination.
