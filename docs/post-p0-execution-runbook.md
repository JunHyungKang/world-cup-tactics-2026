# Post-P0 Execution Runbook

Status: `CURRENT IMPLEMENTATION VERIFIED — PLAN PACKAGE GREEN / EXTERNAL OWNER ACTIONS PENDING`

Planning deadline: `2026-07-27T10:00:00+09:00`

Final deadline: `2026-08-03T10:00:00+09:00`

This runbook converts the selected Corner Policy Lab contracts into one critical
path. It does not authorize external posting, deployment, publishing, or
submission. Human participant recruitment is unavailable and is not a required
claim for this entry; synthetic reviewers cannot replace it. The portfolio board
and canonical eligibility files remain authoritative.

## Current verified state

The original implementation-entry checklist is retained for provenance:

1. `PASS — LOCAL FREEZE` — the Media Statistics v11 PDF and safe reproduction ZIP
   are frozen and verified; external upload/final-save remains an owner action;
2. `PASS` — ran
   `/Users/jhkang/code/competitions/activate.sh world-cup-tactics-2026` after the
   board-recorded freeze; any earlier activation is not current authorization;
3. `PASS` — the official DAKER Data-tab no-year-restriction route is evidenced and the
   unsent scope question is withdrawn;
4. `PASS` — `node scripts/check-eligibility.mjs --promotion` passes after the selected
   source bundle satisfies the data contract without changing
   historical evidence into a 2026 tendency or adding a decorative year label.

Current result as of 2026-07-19 KST: the local P0 artifacts are frozen, the
World Cup repository is activated, the official no-year-restriction route is
canonical, and both Figshare sources are accepted. Corner Policy Lab is the
selected root product. Its current evidence is 7/7 policy data/release, 7/7
source interaction, and 12/12 static four-environment browser PASS. The official
competition page was rechecked on 2026-07-19 KST;
the Jul 27 plan deadline, Aug 3 final deadline, three final URLs, keyless browser
access, licensing, and no-late-commit rules are unchanged.

Stages 0–6 below are historical completed gates, not work to repeat. The active
plan path is account-access verification, owner upload, and owner receipt. The
refreshed exact-artifact Policy Lab PDF review and plan preflight pass. The local P0
freeze is complete; its external owner upload remains separate. The active final
release path requires a fresh clean-commit, deployment, browser, and video
evidence cycle.

## Human and external actions to reserve now

These are not agent-inferred permissions. Each requires the named human action.

| Latest useful time | Owner | Action | Why it is on the critical path |
|---|---|---|---|
| unavailable | none | do not recruit or backfill participant cohorts | preference, usability, and memorability remain unavailable/pending; the plan and final copy make no such claim |
| 2026-07-23 18:00 KST | owner/DAKER account | in a logged-in browser, verify that the account is registered for this challenge and that the planning-submission surface is reachable; record only PASS/PENDING locally, never an email or account identifier in the public repository | public rules and API state cannot prove account-specific participation or upload access; this check must finish before the PDF review/upload window |
| 2026-07-27 08:30 KST | owner/DAKER account | reserve the planning PDF upload, final-save, and confirmation slot | Stage 9 cannot close an external submission with an agent or automated check |
| 2026-08-01 09:30 KST | owner/external accounts | reserve deployment, public GitHub, and YouTube access through Aug 2 10:00 | the fresh release, live surfaces, video, owner submission, and receipt rerun must close inside the 24-hour eligibility TTL |

If these windows are missed, record the missed dependency instead of fabricating
an approval, visual QA row, URL, or external confirmation. A later voluntary
participant study may use the frozen neutral protocol, but it is not on the
submission critical path and cannot delay the current no-human evidence package.

## Route decision

### Route O: official open year scope — selected

The first-party Data-tab record explicitly permits any year, tournament, and
player composition. Proceed with clearly labeled 2018 historical evidence and
independently licensed sources. The withdrawn board question remains unsent.
This route resolves competition scope, not third-party data rights.

### Route H: organizer-confirmed hybrid — fallback

Proceed only when the live official answer explicitly permits rights-cleared 2026
match context plus clearly labeled historical World Cup tactical evidence. Bind
the official response, owner semantic review, OpenFootball 2026 context, and both
Figshare 2018 sources exactly as required by
`docs/data-scope-eligibility-contract.md`.

### Route T: admitted 2026 tactical-only

Proceed only when an explicit-rights 2026 tactical source passes the full
acceptance bundle and materially drives the manager decision. Remove the
historical core and satisfy every `2026-tactical-only` binding. OpenFootball
fixtures cannot satisfy this route.

### Neither route passes

Do not implement a disguised historical product, change only a scope marker, or
submit the current planning draft as eligible. Keep promotion red and escalate
the go/no-go decision to the owner. The planning draft may be used for review but
is marked `NOT FOR SUBMISSION` on every page.

### Canonicalization and freshness

Before source transforms begin:

- Route O captures the official API stable fields, exact no-year-restriction
  quote, updated timestamp, hash, and independent review;
- Route H, only if Route O is superseded, captures the live DAKER response bytes, exact positive quote,
  author/organizer role, URL, and owner semantic review;
- Route T verifies the source-specific six-use rights evidence and marks the
  organizer question `WITHDRAWN — NOT NEEDED` without inventing an answer.

Before stage 3 finishes, materialize every canonical resolution, question,
official-state, product-selection, thesis/planning marker, source artifact, and
public product/data binding required by the eligibility contract. Do not promote
from an alternate path or temporary fixture.

Within 24 hours before plan preflight, and again before the final release commit,
refresh `docs/official-state.md` and the Data-tab stable-field check; for Route H
also refresh the live-answer check,
tracked capture/hash, and owner semantic review. Rerun strict promotion before
committing the release so `submission-build.json` binds those fresh canonical
bytes. The last post-`final-submitted` preflight must also finish within 24 hours
of every TTL-bound check. If it cannot, the release is stale: refresh canonical
evidence, create a new release commit, and repeat all bound build/deploy/browser/
video evidence rather than editing eligibility files after release.

## Jul 23–27 planning critical path

The times below are maximum focused budgets, not evidence that work is complete.
Every stage ends in a gate; unused time goes to the next stage, not new features.

| Order | Focus budget | Work | Exit evidence | Hard stop |
|---:|---:|---|---|---|
| 0 | bounded P1 | P0 freeze remains pending; official rules/Data tab rechecked | board keeps this project P1; canonical Route O scope evidence remains valid | no deployment, feature expansion, or final release before P0 freeze |
| 1 | complete | selected source transforms and checksum/schema tests | versioned deterministic transform and receipt | source hash/schema/population drift |
| 2A | complete | independent football structural and semantic review | `42/42` structural and semantic PASS | any row or review digest drift |
| 2B | complete | data-compliance rights/capability/lineage review | accepted Figshare evidence bundles | rights/capability ambiguity or digest drift |
| 3 | complete | canonical route, minimal public artifact, source and product binding | two accepted records and strict promotion PASS | raw/extra data shipped or binding drift |
| 4 | complete | domain validator, immutable evidence, reducer, failure boundary | deterministic unit/contract suite | synthetic fallback, runtime API/key/randomness |
| 5 | complete | first-screen role move, receipt, replay, evidence split, counterexample, switch, reset | current production component and browser evidence | legacy shell or semantic regression |
| 6 | complete pre-release | pointer/touch-button/keyboard, transcript, focus, reduced motion, responsive hierarchy | 56/56 four-project pre-release PASS | BG-12 remains release-only; physical touch/VoiceOver stays unavailable/no-claim |
| 7 | unavailable / no claim | preserve the frozen neutral protocol without recruiting or fabricating participants | pending evidence file and explicit absence of preference/usability/memorability claims | any synthetic or agent observation presented as participant evidence |
| 8A | pending account gate | owner verifies an already logged-in DAKER account is registered for this challenge and can reach the planning-submission surface; keep identity details out of the public repository | owner-observed local PASS before the upload window | public-page inference, agent-created account, unconfirmed registration, missing upload surface, or personal account data committed to Git |
| 8B | complete exact document gate | `docs/reviews/plan-visual-agent-review-2e1c9a477203ccea.json` binds all eight current Policy Lab pages, source, packet, renderer, criteria, and zero findings | SHA-bound independent-agent `plan-visual-qa` row and canonical review JSON | relabeling as human evidence, stale/tampered PDF/packet/review, or any visual defect |
| 9 | pending external | after 8A and 8B pass, rerun verification and plan preflight, regenerate `pnpm planning:handoff`, then owner uploads the exact PDF and records confirmation | green owner handoff plus `plan-submitted` owner receipt | account preflight PENDING/FAIL, any other PENDING/FAIL, locked handoff, missing valid review row, owner slot unavailable, missing official confirmation, or time past buffer |

Owner buffer: finish the upload and final save by `2026-07-27T09:00:00+09:00`,
one hour before the hard deadline. Stages 0–6 are already complete and must not
be reopened as feature work. The remaining plan path is intentionally narrow:
one account-access preflight, one exact preflight, and one owner upload/receipt.
The independent document review is already complete. No time saving may trade
away an evidence gate.

Planning verification:

```bash
pnpm verify
pnpm submission:preflight -- --phase plan \
  --planning-pdf output/pdf/corner-policy-lab-planning.pdf
```

Expected proof is not merely command exit zero: strict eligibility, accepted
data, final PDF content/render, and the SHA-bound independent-agent visual ledger must all be green.

## Jul 27–Aug 3 final critical path

| Date buffer | Work | Required evidence |
|---|---|---|
| Jul 27–29 | resolve study/browser defects; visual polish only after semantic gates | regression tests for every fixed defect; no feature expansion |
| Jul 30 | pre-freeze four-project, artifact-visual, and timed 60-second rehearsals | defects fixed before release; rehearsal output cannot enter the final ledger; VoiceOver stays unavailable/no-claim |
| Aug 1 10:00 | refresh canonical eligibility/TTL, pass strict promotion, create and push the clean release commit, build its exact `dist`, and deploy it | public release SHA, complete-dist digest, HTTPS keyless URL, deployed-file parity; TTL clock recorded |
| Aug 1 13:00 | run deployed final browser matrix, then dispatch a distinct non-creator artifact reviewer | BG-01–BG-15, zero skips/failures, exact release/report binding, zero-findings review JSON |
| Aug 1 16:00 | record the timed 60-second and full-flow video only from the frozen URL; publish YouTube and verify public GitHub/deployment/YouTube surfaces | three-frame cold open, role move, immediate `내 약속`, replay, reactive/fixed distinction, duty switch/reset, unexplained-shot counterexample/source ID final frame, exact release parity |
| Aug 2 08:00 | append four final rows in evidence-time order, commit/push only the ledger, run final preflight on clean HEAD, submit when green, then append/commit/push the `final-submitted` receipt and rerun final preflight by 09:30 | public deployment/GitHub/YouTube, frozen release, owner-observed confirmation, clean ledger-only post-release commits, and final PASS inside the 24-hour TTL |
| Aug 2 10:00 | submission/final-save, receipt, or full fresh-release recovery decision; no piecemeal eligibility edit | exact confirmation remains recorded, or the entire release/evidence cycle restarts with fresh canonical bytes |
| Aug 3 09:00 | owner final-save confirmation only; no code changes | external confirmation recorded before one-hour buffer |

Release order is intentionally strict:

1. refresh canonical eligibility/TTL and rerun strict promotion;
2. make and push the clean release commit to the public repository;
3. build with
   `pnpm submission:build -- --release-commit <FULL_RELEASE_SHA>`;
4. deploy exactly that `dist/`;
5. run
   `pnpm test:e2e:final -- --deployed-url <URL> --release-commit
   <FULL_RELEASE_SHA> --build-sha256 <DIST_SHA256>`;
6. record and publish the demo from that exact deployment;
7. run `node scripts/prepare-final-artifact-review.mjs`, dispatch a distinct non-creator subagent over
   all 19 PNGs, and finalize the content-addressed review;
8. verify the GitHub, deployment, and YouTube public surfaces;
9. record the owner's content-addressed YouTube upload attestation;
10. append the four evidence rows in timestamp order `final-browser-qa` →
   `github-public` → `youtube-public` → `final-freeze`;
11. commit and push only the ledger and two content-addressed evidence JSONs after the release commit;
12. run final preflight with the deployed, GitHub, YouTube, release, browser
   report, demo manifest, and artifact-review arguments;
13. submit and have the owner observe the official final-save confirmation;
14. append the exact `final-submitted` receipt, commit/push only the ledger,
    rerun final preflight on clean HEAD with `--require-final-submitted true`, and make no commit after
    `2026-08-03T10:00:00+09:00`.

Any defect found after the Aug 1 release invalidates its deployment, browser
report, video, and ledger bindings. Fixing it requires a new clean release commit
and repeating steps 3–12. Do not patch the live site or reuse evidence from the
old release; Aug 2 is the recovery ceiling, not planned feature time. The same
full restart applies if the final post-receipt preflight would run after the
eligibility TTL expires.

Generate the exact upload candidate from the frozen public release:

```bash
pnpm demo:rehearse -- \
  --deployed-url <DEPLOYED_HTTPS_URL> \
  --release-commit <FULL_RELEASE_SHA> \
  --build-sha256 <DIST_SHA256> \
  --output submissions/final-demo-visual.webm \
  --manifest submissions/final-demo-visual.json
pnpm demo:rehearsal:audit -- \
  --manifest submissions/final-demo-visual.json
pnpm demo:narrate -- \
  --visual-manifest submissions/final-demo-visual.json \
  --output submissions/final-demo.webm \
  --manifest submissions/final-demo.json
pnpm demo:narration:audit -- \
  --manifest submissions/final-demo.json
```

The visual command refuses a dirty/non-release HEAD, verifies the local stamped
marker against every deployed file, and records the continuous interaction from
that canonical HTTPS URL. The five-second title card remains the exact
release-tree gallery asset and is separately hash-bound; it is not described as
a public-app screenshot. The narration command inherits the frozen URL,
release/build, marker, capture, story, and cold-open bindings. Its TTS remains a
candidate until the SHA-bound audio/caption/demo-contract artifact review
passes, and publishing to YouTube remains an external owner action.

The canonical report path from `playwright.final.config.ts` is
`artifacts/final-playwright-report.json`. The final command shape is:

```bash
pnpm submission:preflight -- --phase final \
  --deployed-url <DEPLOYED_HTTPS_URL> \
  --github-url <PUBLIC_GITHUB_URL> \
  --youtube-url <PUBLIC_YOUTUBE_WATCH_URL> \
  --demo-video <EXACT_UPLOADED_VIDEO_FILE> \
  --demo-manifest <EXACT_UPLOAD_CANDIDATE_MANIFEST> \
  --artifact-review <SHA_BOUND_INDEPENDENT_AGENT_REVIEW_JSON> \
  --youtube-upload-confirmation <CONTENT_ADDRESSED_OWNER_ATTESTATION_JSON> \
  --release-commit <FULL_RELEASE_SHA> \
  --browser-report artifacts/final-playwright-report.json
```

After the owner observes the official final-save confirmation and the
`final-submitted` ledger-only commit is public, rerun the same command with
`--require-final-submitted true`. The post-submit mode fails closed if that exact
owner receipt is absent; the pre-submit run intentionally leaves it optional.

## Agent work packets

Parallel work is allowed only across independent evidence owners:

| Packet | Owner role | Files/surface | Cannot approve |
|---|---|---|---|
| DATA | transform implementer | transform, source tests, minimal derived artifact | own semantic audit or rights review |
| FOOTBALL | independent reviewer | every source window/row, wording and region semantics | transform implementation |
| DATA-COMPLIANCE | named independent reviewer | source URL/licence, six uses, capability/schema, lineage, minimization, attribution, executed-test receipt | transform implementation or anonymous/status-only approval |
| PRODUCT | main implementer | domain state, app, interaction tests | fresh-user results |
| UX | independent fresh participants/reviewer | comprehension, delayed recall, visual hierarchy | source acceptance |
| BROWSER | independent adversarial reviewer | real production browser matrix and artifact QA | VoiceOver, human usability, or participant evidence |
| SUBMISSION | independent compliance reviewer | URLs, ledger, release/freeze bindings | external submission confirmation |

The main agent integrates results and fixes failures. No reviewer may replace an
external human approval, account action, or independent participant result with a
chat-only assertion.

## Daily command floor

Run at every merge point:

```bash
pnpm verify
node scripts/check-eligibility.mjs --promotion
git diff --check
```

Near plan freeze, add `pnpm planning:draft:inspect` or the final plan preflight.
Near final freeze, add production `pnpm test:e2e`, release build, public browser,
and final preflight commands. A green narrow test never substitutes for a broader
required gate.

## Scope locks

Until the plan is submitted, the only product slice is:

- one opponent evidence corpus;
- one outlet role with keep-high plus four corner duties;
- immediate `내 약속` after the role move;
- explicit source-linked contact replay;
- separate reactive and immutable evidence;
- one unexplained-shot counterexample per available duty;
- one duty switch and reset;
- no runtime LLM, chat, formation builder, player roster, score, xG, win
  probability, live API, team crest, flag, or decorative 3D mode.

After plan submission, add no feature unless it closes a documented browser,
comprehension, reliability, or submission defect. Visual polish cannot change the
evidence semantics or release binding.

## Completion definition

The runbook is complete only when the official planning PDF and all three final
URLs have been submitted, the ledger binds them to the same allowed source scope
and release, the public app passes the real manager loop without login/key, and
the repository is frozen before the deadline. Until then this document is a
critical-path map, not completion evidence.
