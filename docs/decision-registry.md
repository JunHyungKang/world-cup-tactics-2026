# Decision Registry

| ID | Decision | Status | Evidence | Revisit condition |
|---|---|---|---|---|
| D01 | Use React, TypeScript, Vite, Vitest, and Playwright for a static deployable client | accepted | Official Vite React/TypeScript template support; no server or judge key required | A selected concept requires server-only computation |
| D02 | Keep the initial tactic coefficients visibly labeled as prototype logic | accepted | No football dataset has passed the source/license gate | A tested calculation is connected to an accepted dataset |
| D03 | Select the final product thesis now | accepted | Corner War Room is bound to two admitted CC BY 4.0 sources, a deterministic public transform, and a 42/42 independent audit | Reopen only if implementation cannot preserve the claim boundary or core manager loop |
| D04 | Treat formation drag-and-drop alone as the differentiator | rejected | Official examples already name this baseline; weak differentiation | Never, unless paired with a stronger decision engine |
| D05 | Select "Corner War Room" as the product thesis | accepted | Independent football/data/UX reviews converge on real-event stress testing; the one-opponent transform, calculation, corrected sensitivity, and full audit now pass | Reopen if the production interaction or neutral five-second comprehension test fails |
| D06 | Describe the intervention as changing the match outcome or xG | rejected | Event-level 360 frames are not continuous tracking and do not identify the unobserved counterfactual | Only if a separately validated model and appropriately licensed training data support the claim |
| D07 | Make the data/decision engine static and deterministic | accepted | Keyless judging, reproducible receipt, and reliable deployment are higher value than runtime generative output | A later feature demonstrates material judging value without adding secret or availability risk |
| D08 | Use StatsBomb Open Data in the public prize entry | hold | Discovery found 64 WC 2022 matches with event and 360 files, but the Public Data User Agreement restricts raw-data distribution and commercial use | Only with written permission covering prize-entry use and public derived output |
| D09 | Use the Figshare Soccer Match Event Dataset as the primary source | accepted | Exact archive/extracted hashes, CC BY 4.0 rights receipts, deterministic transform tests, public lineage, and independent 42/42 structural/semantic review pass | Re-audit if source bytes, transform, public derived hash, or claim boundary changes |
| D10 | Replace Corner War Room with a FIFA 2026 freehand pressing-trap product now | rejected | PMSR aggregates do not provide arbitrary trap-zone coordinates; through/around/over are line-break methods rather than pitch channels | Revisit only as the constrained Press Pact after D11 passes |
| D11 | Develop 2026 Press Pact as a current-data challenger | revise | Strongest current/emotional framing and useful official aggregates, but prize-entry reuse rights and three-report reproducibility are unproven | Written FIFA permission plus three reports, 99% extraction audit, plan/evidence separation, and five-second comprehension pass |
| D12 | Use the Mominullptr 2026 dataset as licensed tactical evidence | rejected | Upstream rights are unresolved, provenance is domain-level, metrics differ by provider, and inspected possession pairs contain inconsistencies | Only if every used row gains authoritative upstream permission and reproducible source mapping |
| D13 | Use OpenFootball 2026 for current fixture context | pending | Upstream/generated pins and CC0 pass; JSON SHA `688738…6c30` has 104 canonical tuples and 48 teams, but is manual/not-live and lacks explicit status | Strip scores/goals; accept only after canonical-key, nullable-number, fixed-offset, snapshot/staleness, and isolation tests; re-pin before both freezes |
| D14 | Score four defensive zones and the counter outlet with one coverage metric | rejected | At 10 seconds, 38/42 windows contain defending-team events, 16 contain 17 clearances, and short-option endpoints are 14/42; mixing attack and outlet evidence creates a false football score | Keep Brazil priority contact and defending-team outlet-band evidence in separate fixed columns with no composite score |
| D15 | Make one outlet-player role move the primary direct manipulation | accepted | Production pointer/touch-button/keyboard interaction and 56/56 four-project pre-release contracts pass; human comprehension and effectiveness remain unevaluated and unclaimed | Preserve input parity and claim boundaries through the stamped public BG-12 release |
| D16 | Use an inclusive 10-second recorded corner window | accepted | 8/10/12/15 seconds all retain 11 Brazil-shot windows; outlet-band contact reaches its 10-window plateau at 10 seconds, while 12 seconds adds only one second-ball contact and 15 seconds shows later-play drift | Revisit if the repository transform contradicts the one-off sensitivity table |
| D17 | Use one global or nominal-only Skeptic example | rejected | Near-post-side and central-to-far candidates flip across the 81 boundary variants | Select the earliest shot window outside the chosen priority under every tested variant, or state that no robust candidate exists |
| D18 | Show the complete endpoint receipt and Council controls before the first move | rejected | Independent judge review found the first viewport demanded six concepts in five seconds and risked another generic tactics board | First viewport has one role tradeoff, four mission slots, and one limitation badge; reveal receipt/evidence after the move |
| D19 | Present Scout, Coach, and Skeptic as three equal tabs or live AI agents | rejected | Tabs add navigation and make the deterministic workflow look like an AI-agent demo | Use `관찰`, `내 약속`, and one primary `반례 보기` action; keep role names secondary |
| D20 | Use the original leading five-user questions as comprehension evidence | rejected | Questions named the decision, changed/fixed distinction, panel layout, and claim boundary before scoring | Use neutral unassisted prompts, separate spontaneous discovery from guided controls, and add three-minute comparison recall |
| D21 | Treat same-participant War Room/baseline recall as validated differentiation | rejected | Prior exposure, guided training, unequal timing, and unmatched visual finish bias recall | Use a separate fresh counterbalanced cohort with matched 45-second scripts, or label the comparison exploratory |
| D22 | Label defending-team Pass/Clearance records as a transition or successful outlet | rejected | The fixed channel contains recorded event segments, not proof of a completed transition or user-caused counterattack | Label it `당시 수비팀 걷어내기·패스 기록 · 고정` and keep it separate from Brazil contact evidence |
| D23 | Treat reachable HTTPS URLs and handwritten PASS rows as final submission proof | rejected | Adversarial review showed that stale assets, synthetic browser JSON, future rows, private hosts, and a public but incomplete repository could otherwise pass | Require the release build manifest, deployed per-file hashes, source-bound Playwright report, ordered evidence ledger, public DNS/API checks, and repo-docs gate |
| D24 | Wait for accepted data before testing the planning document's page architecture | rejected | Layout risk can be retired without fabricating evidence by rendering a submission-shaped draft whose every page is visibly draft, data-audit-pending, and implementation-pending | Promote to final only after product/data/implementation gates pass and human `plan-visual-qa` is bound to the final PDF and source hashes |
| D25 | Treat a 2026 fixture label beside 2018 tactical evidence as sufficient theme compliance | rejected | The official headline says `2026 FIFA 월드컵 데이터를 활용`, while fixture-only OpenFootball cannot support tactics and historical eligibility is unanswered | Plan/final preflight stays blocked until an official DAKER answer confirms the hybrid route plus all three sources are admitted, or the thesis pivots to admitted 2026 tactical evidence only |
| D26 | Treat typed status, URL, reviewer, PASS receipt, or scope marker fields as eligibility evidence | rejected | Independent QA promoted fabricated override files, answer-shaped URLs, status-only sources, unexecuted receipts, and marker-only pivots through the early validator | Canonical-only preflight must bind live official bytes, structured rights/capability artifacts, executed tests, public data lineage, product selection, and release hashes |
| D27 | Replace Corner War Room now with a 2026-tactical-only product backed by a public API | rejected | A bounded source gate found potentially relevant endpoint surfaces whose actual records and tactical sufficiency are unaudited and whose terms do not establish the full public prize/GitHub/app/YouTube/derived-output rights bundle; the open-data candidate is match-and-goal context only | Recheck once after the 2026 final and before each freeze; reopen for an explicit six-use licence, source-specific written permission, a DAKER-designated dataset, or an official hybrid-scope confirmation |
| D28 | Lead the pitch with three AI analyst roles or self-awarded 5/5 scores | rejected | Independent football and judge-UX gates found that the memorable user event is a scarce role allocation attacked by an unexplained shot record; analyst labels are internal vocabulary and current evidence is document-level | Lead with the one-role loss and counterexample; report current proof versus target proof, then validate recall with fresh users |
| D29 | Start UI implementation immediately after P0 freeze even if data scope is unresolved | rejected | Planning and final preflight require strict canonical eligibility; building the selected interaction first would consume the plan window on a thesis that may be ineligible | Post-P0 order is route proof → accepted data/audit → domain → replacement UI → users/browser → final PDF; keep the app untouched while neither H nor T passes |
| D30 | Treat a hash-bound, free-form visual-QA ledger line as proof of independent review | rejected | Adversarial review showed that future, duplicate, malformed, contradictory, or placeholder-reviewer rows could satisfy substring checks | Parse an exact seven-cell contract, bind time/path/hashes/reviewer role, reject non-pass tokens, and retain the actual eight-page inspection as a human boundary |
| D31 | Submit the historical-core plan after organizer silence as long as the limitation is disclosed | rejected | The official headline still says `2026 FIFA 월드컵 데이터`; disclosure prevents overclaiming but does not prove eligibility, and strict promotion correctly remains red | By 2026-07-23 18:00 KST require a qualifying Route H answer or an admitted Route T replacement; otherwise escalate plan-submission go/no-go to the owner |
| D32 | Keep canonical question state as `draft-not-sent` after the owner posts because eligibility is still unresolved | rejected | The posting packet exposed that the v2 state machine could represent only unsent or answered, forcing an honest post to fail audit or disappear from canonical evidence | Schema v3 adds content-bound `posted-awaiting-answer`: exact post URL, capture, canonical message, A-proposition owner review, and Git tracking; it remains unresolved until H/T passes |
| D33 | Make a learned recommendation or synthetic optimality score the research-led differentiator | rejected | TacticAI and recent role-inference work depend on unavailable tracking data and models; Action-Evaluator shows that choice, context, and explanation are more useful than an isolated score | Revisit only with rights-cleared tracking data, a validated model, and product evidence that the score improves judging without inviting causal misreading |
| D34 | Interpret Apple-style design as pervasive glass, oversized branding, and decorative motion | rejected | Official HIG principles and the professional-persona review prioritise purpose, hierarchy, agency, accessible redundancy, and restrained functional materials | Use translucency only for a clearly separate control layer after contrast, reduced-motion, forced-colours, and `320x568` first-fold tests pass |
| D35 | Replace the Touchline Lab shell with a pitch-centred Corner War Room interaction after the data gate | accepted | The replacement now passes the six-element `320×568` fold, deterministic `선택 -> 기록 -> 반례 -> 재선택/초기화` loop, 78/78 unit/contract checks, and 56/56 four-project pre-release gates | Preserve the current interaction; BG-12 stays pending for the stamped public release and human comprehension remains unavailable/no-claim under D38 |
| D36 | Require a DAKER board answer before using clearly labeled historical World Cup evidence | rejected | The live first-party Data-tab record explicitly states there is no restriction on a specific year, tournament, or player composition; the prior audit missed this more specific rule | Reopen only if the official Data-tab wording changes or an organizer notice supersedes it |
| D37 | Treat the official no-year-restriction rule as granting third-party data rights | rejected | DAKER separately requires commercially usable licensing for external APIs; competition scope and source copyright are different gates | Every selected external source still needs its own rights, transform, test, audit, derived-lineage, and attribution evidence |
| D38 | Add another feature or dataset before fresh-user proof | rejected | Data, implementation, and 56 pre-release browser contracts now pass; the largest remaining uncertainty is whether uncoached users understand the scarce-role tradeoff, avoid causal misreads, and remember the counterexample | Reopen only after primary `4/5`, misconception `<=1/5`, and delayed-recall `3/5` gates pass |
| D39 | Reopen one bounded product challenger after the owner's RL/ontology critique | accepted | The owner identified that the current loop can feel like retrieval and cannot judge a choice; the exploration is isolated from the verified product and starts with a fail-closed data spike | Stop at the spike if population, support, sensitivity, or claim gates fail; do not mutate the submission candidate first |
| D40 | Present the current defensive-duty move as an offline RL action | rejected | The source has zero logged defensive assignments, marking states, action propensities, or defensive counterfactual rewards across 603 corners | Only with rights-cleared tracking plus logged or defensibly inferred actions, overlap, and validated reward/OPE evidence |
| D41 | Use an ontology to fill missing actions or causal effects | rejected | Ontology types observations, assumptions, provenance, and prohibited relations but cannot create counterfactual evidence or sample support | Use it only as the state/action/outcome contract, support mask, provenance layer, and short explanation path |
| D42 | Explore Corner Policy Lab as an ontology-constrained policy rehearsal | rejected | All 603 corners can be typed, but 46 delivery endpoints are placeholders, leaving 557 observed actions below the 95% gate; team support, full horizon ranking, and top-vs-runner separation also fail | Keep the isolated prototype as falsification evidence; retain Corner War Room unless a different action source passes rights, observability, support, holdout, and sensitivity gates |
| D43 | Rebuild Policy Lab as a fixed tournament-stage empirical campaign | revise | The v4 candidate fixes 48 group-stage reference, 8 round-of-16 evaluation, and 8 quarter-final-and-later final-audit matches; it stores evaluation receipts without claiming revisions, exposes abstention and ontology contradictions, and passes 7/7 data/release, 7/7 Chromium interaction, and 12/12 static four-environment contracts | Promote only if an exact-artifact independent re-review scores above Corner War Room 92/100 without causal, sequence, ontology, or submission-binding mismatch |
| D44 | Treat the first exact-artifact Policy Lab video/PDF package as promotion-ready | revise | Independent review scored 91/100 versus Corner War Room 92 and found four internal mismatches: 60.088-second `under 60` wording, sidecar-only captions, ontology node-name drift, and stale D43 evidence; these were subsequently corrected in a 59.520-second caption-burned candidate with schema-aligned paths | Re-score the new exact hashes; keep Corner War Room official unless the bounded candidate exceeds 92 and external submission bindings are authorized |
| D45 | Promote the corrected Policy Lab over the current exact Corner War Room package | rejected | A same-reviewer, exact-artifact comparison without inherited scores rated Corner War Room 97/100 and Policy Lab 95/100. Policy Lab led originality 30/30, but Corner War Room led manager experience and planning-to-implementation consistency because its +1/-1 role move, drag, replay, reset, and counterexample are more immediate and already canonical. Both lacked the same public and human evidence. | Keep Corner War Room official through P0 and submission; retain Policy Lab as a 59.520-second, SHA-bound research challenger and reopen only if a materially shorter direct loop beats the exact canonical package without a conversion tax |
| D46 | Keep the 60.088-second sidecar-caption rehearsal as the official demo candidate | rejected | The regenerated local rehearsal is 59.928 seconds, burns the SHA-bound Korean SRT into the VP8 video, retains Opus narration, passes the narration audit, and preserves the important interface regions in inspected 2s, 16s, 45s, and 58s frames | Regenerate the same sub-60, burned-caption pipeline only from the frozen public release before YouTube upload; local rehearsal remains no-claim evidence |
| D47 | Accept Policy Lab's 13-action campaign as the shortest credible judging path | rejected | The v5 Quick Trial uses five activations: two priority choices, one immutable policy lock, one 16강 summary reveal, and one same-policy sealed-final reveal. Eight receipts carry the same fingerprint, selection stays disabled, and the final receipt records zero revisions. Source Chromium passes 7/7, the built candidate passes 12/12 across four environments, and the exact 59.520-second caption-burned video reaches the final receipt at 34.008 seconds with one explicit scroll. | Require an exact-artifact independent comparison of video `c414c0b0…`, the regenerated PDF, and release `30633bbf…`; no promotion or external submission claim before that gate |
| D48 | Promote the one-lock Corner Policy Lab v5 as the official product candidate | accepted | A same-reviewer comparison scored the copy-corrected Policy Lab 98/100 versus Corner War Room 97/100. Product selection is explicitly `PASS` while causal recommendation remains `REJECT` and the empirical campaign remains `REVISE`. The canonical root release `2f761b98…`, narrated video `558e8f0b…`, capture manifest `14ef0eed…`, eight-page PDF `2e1c9a47…`, story `cde50e49…`, and gallery `a98f9517…` preserve five activations, one immutable `P-7391425c` lock, one explicit scroll, a 34.005-second final receipt, `7/7` source contracts, and `12/12` four-environment release checks. The refreshed plan review `98f63fb9…` and plan preflight pass. | The 18-artifact CWR archive is frozen. Next bind a clean release commit, public GitHub, hosting, deployed browser evidence, YouTube, and DAKER receipts; human evidence remains unavailable/no-claim. |
| D49 | Use a manually triggered GitHub Pages workflow for the public static release | accepted | The canonical app already builds a portable relative-URL `dist/`; the workflow uses the official Pages configuration, upload, and deployment actions, grants only read/Pages/OIDC permissions, runs the locked build and deployment audit, and uploads only `dist/`. Three workflow contracts and the full 90-test suite pass without changing the canonical release or deployment digests. | Create the public repository and enable Actions as the Pages source only after owner approval; do not claim deployment until BG-12 fetches the stamped HTTPS bytes. |
| D50 | Treat the first public Pages release as candidate smoke evidence, not final BG-12 | accepted | Public GitHub and HTTPS Pages are live; five files match local SHA-256 values, and Chromium, Firefox, WebKit, and mobile complete the one-lock 48–8–8 loop with zero automated accessibility violations. The deployed bytes still identify themselves as a candidate and do not include the final `submission-build.json` marker. | Promote only after the final release commit is stamped, redeployed, verified by BG-12 and the twelve-screenshot browser report, then bound to the frozen-URL video, YouTube, and DAKER receipts. |
| D51 | Finalize the uploaded planning draft without revising its judge-facing story | rejected | Independent review scored the exact eight-page draft 78/100: page 3 made the decisive contradiction unreadable, page 8 used self-awarded 98/100 language, public-state copy was stale, and the loop ended at evidence consumption instead of a manager response. | Replace the saved DAKER draft with a re-reviewed PDF that enlarges the contradiction, removes self-scoring, reports current public state, and binds a separate next-meeting decision that cannot mutate the sealed policy or results. |
| D52 | Add one predeclared location-overlap criterion before the policy lock | accepted | Independent first-place review found that two attention areas without a success criterion could reinterpret any held-out overlap after the fact. The manager now chooses 40%, 50%, or 60% before either holdout is exposed; the same fingerprint binds areas and criterion, and each result closes as `사전 기준 충족` or `사전 기준 미달`. This remains a location-inspection criterion, never defensive success, shot prevention, OPE, or optimality. The product gate passes because the criterion changes the lock, receipt, counterexample interpretation, and 60-second demo while using only licensed observed locations. | Preserve abstention and causal `REJECT`; reject any wording that turns the criterion verdict into match success or tactical effectiveness. |
| D53 | Treat a green local release build as proof that the public clone can deploy | rejected | The first exact Pages run failed because default tests consumed ignored raw data, local video/PDF/handoff artifacts, Python packages absent from the runner, and a candidate-only build manifest. The corrected isolated snapshot now installs fresh locked JavaScript dependencies and passes 98 raw-free tests plus the stamped builder from 250 tracked/nonignored files; exact raw, local media, PDF, and receipt checks remain explicit fail-closed lanes. | Require `pnpm submission:drill` before every deploy and preserve the tracked-only default-test contract; a local cache or ignored artifact cannot satisfy public-release proof. |
| D54 | Add cinematic or generated football scenes to make the demo look more professional | rejected | The judging proof is the real manager loop. Generated scenes would consume scarce seconds, weaken product-to-video fidelity, and risk implying unsupported match outcomes. The accepted treatment keeps the product recording primary, adds a restrained slow zoom, splits the first audit into verdict → receipts → counterexample → seal, masters speech to a broadcast-safe target, and labels every post-production HUD `[편집 요약]`. | Reopen only if a real licensed match clip materially clarifies the interaction without replacing product evidence or crossing the result/causal claim boundary. |
| D55 | Spend the final planning window on a cold-eye structural rewrite rather than more product validation | accepted | External red-team review found that the draft explained analytical rigor better than managerial agency: page 1 led with method, page 5 spent a full page on ontology, and page 7 led with test counts. The official second-round rubric gives manager experience 25 points, while the first round is voting-led. The revised plan therefore opens with two scarce attention tokens, compares the loop against dashboards/recommenders/boards, makes the set-piece meeting and three decisions explicit, and compresses technical proof into supporting evidence. | Freeze product scope. Regenerate and independently inspect one eight-page PDF, then move immediately to the owner-controlled DAKER planning upload gate; no new feature, data source, or unbounded optimization lane before the 2026-07-27 10:00 KST deadline. |
| D56 | Accept the cold-eye eight-page planning package as the canonical upload candidate | accepted | The first exact review found two major inconsistencies and one minor crop issue: fingerprint lineage was overstated, the standalone PDF lacked full CC BY attribution, and page 3 cut relations 5–7. PDF `060376c8…` now states the implemented fingerprint inputs, separates receipt provenance, names Pappalardo/Massucco, Wyscout, Figshare, CC BY 4.0, Events/Matches URLs and project transformation, and shows all seven evidence relations. The exact 8/8 re-review passes blocker=0, major=0, minor=0; all 99 repository tests and the full verification chain pass. | Replace only the reversible DAKER draft after owner confirmation, verify the remote PDF hash, and keep the final submit click as a separate confirmation gate. |
| D57 | Name the two attention tokens as a defensive leader and an outlet role, with an optional one-area plus outlet-retained branch | accepted | Independent rubric red-team found the original two-area overlap choice had a dominant `니어+중앙·파` strategy and no visible football cost. The v5 transform now derives fixed defending Pass/Clearance outlet-band context in the same ten-second windows (`64/397`, `14/84`, `12/76`), while the UI keeps it separate from location overlap. Both staffing choices bind the fingerprint and receipt; the raw-derived contract is `7/7`, the source interaction contract is `10/10`, and the built four-project loop is `12/12`. The submitted PDF's two-area path remains the primary demo path; outlet retention is a bounded implementation refinement of the second scarce token. | Reject if the two observations are combined into a score, the outlet count changes with the user's move, the app implies a completed counterattack or player reach, or the final story fails to preserve the submitted two-area primary path. |
| D58 | Keep the method-first `정책·봉인·반례·14/84` demo narration after automated and agent review passed | rejected | The owner watched the exact `efae7cfb…` candidate and could not explain what the service does; the Korean sounded artificial. A fresh novice-UX review confirmed that the first five seconds posed a problem without naming the tool, while `놓친 장면` and research vocabulary obscured the action and risked implying real defensive failure. | Lead with the exact choice the tool supports, then show role/zone/criterion selection, result-before-lock, the same choice across two eight-match sets, outside-selection records, the success-rate boundary, and the next-meeting memo. Keep outlet counts visible as secondary product evidence but remove them from spoken narration. |
| D59 | Treat tournament-wide delivery overlap as meaningful team analysis | rejected | The owner identified the decisive football flaw: the released loop knows neither the selected opponent nor a team profile, and sparse tournament counts cannot make a generic two-zone overlap tactically meaningful. The global data gate already has `team_specific_support=false`; 31/32 teams have a delivery category below three observations. | Do not upload the current demo. A replacement must condition on an identified opponent, disclose sample support, and beat a tournament-only forecast on untouched matches before any defensive interaction is presented. |
| D60 | Reframe Policy Lab as a partially pooled opponent-corner scouting rehearsal | revise | A group-stage-only Dirichlet-multinomial model selects concentration `16` without reading either knockout partition. It lowers log loss versus the tournament baseline by `2.14%` on 84 round-of-16 corners and `7.21%` on 76 later corners, `4.59%` overall; Brier improves `4.66%`, 12/16 teams improve, and the 10,000-draw match-cluster 95% log-score-gain interval is `[0.0040, 0.1071]`. This supports team-conditioned endpoint forecasting, not optimal placement or defensive effect. However, compressing each profile to two top areas yields the same aggregate coverage as the tournament top two (`65/84`, `59/76`), so the released universal two-area loop is still unsupported. | Promote only if the bounded UI makes one identified opponent and the full four-way probability profile the decision unit, exposes sample and shrinkage, preserves the submitted split/precommitment/claim boundary recognizably, passes Korean novice comprehension and browser gates, and still fits the remaining release/video evidence budget. |
| D61 | Use the four-way opponent profile to recommend two zones | rejected | Team conditioning improves proper probabilistic scores, but top-two coverage is identical to the tournament baseline across both held-out partitions. A zone recommendation would turn a distributional signal into an unsupported tactical optimum. | Preserve all four probabilities and give no default or automatic allocation. |
| D62 | Prototype a Portugal-specific ten-repetition corner training planner | accepted | The first screen names Portugal, 14 classifiable group-stage corners, 47/53 partial-pooling weights, and all four probabilities. The manager manually allocates exactly ten rehearsal repetitions, locks before the fixed `5/1/4/0` reveal, and receives no score or recommendation. Three Chromium contracts pass, including zero automated accessibility violations; Korean challenger copy has zero high-confidence findings. | Keep it isolated until the immutable planning-PDF consistency and evidence-reset budget pass. On promotion, replace every canonical release/story/video surface and do not inherit any prior public or demo evidence. |
| D63 | Replace the submitted manager loop with the ten-repetition challenger | rejected | Independent submission review found that a full replacement would break planning-to-implementation consistency. The accepted additive implementation instead places a Portugal dossier before the existing two-role choice: 14 classifiable group-stage corners, 47/53 evidence/prior weights, all four probabilities, Uruguay's separate 5/6 defensive-exposure record, and aggregate holdout gains and harms. The role lock, `48 → 8 → 8` campaign, fingerprint, counterexample, and next-meeting decision remain intact. Source interaction passes 14/14 and the built app passes 12/12 across Chromium, Firefox, WebKit, and mobile, including automated accessibility checks. | Preserve the dossier as context, never as a zone recommendation. Reopen full replacement only with a revised planning contract or evidence that the challenger materially outweighs the consistency loss. |
| D64 | Pool the manager team's defensive exposure into the opponent forecast and call it matchup analysis | rejected | A group-only, leave-one-match-out log-linear challenger with fixed concentration `16` selects defensive weight `0.5`. It improves mean log loss by `1.39%` in the round of 16, `0.59%` in quarter-final-and-later, and `1.01%` across all 160 knockout corners, with Brier non-worse in both partitions. However, the 10,000-draw knockout-match-cluster gain interval is `[-0.0040, 0.0274]` and `P(gain > 0)=0.9226`, missing the predeclared lower-bound-above-zero and `0.975` probability gates. A simpler pseudo-count combination also worsens the later partition. | Keep the displayed forecast opponent-only and show Uruguay's `5/6` defensive-exposure record plus the failed challenger gate as separate evidence. Reopen only with more rights-cleared team/tracking data and a frozen challenger that passes every promotion gate without knockout retuning. |
| D65 | Treat the two selected zones as an unordered set in the sealed policy fingerprint | rejected | The first selected zone is labelled the defensive leader and the second is the reassigned outlet role, so reversing the same two zones changes the manager's role assignment. Sorting zones before hashing gave both assignments one receipt ID and violated the immutable-policy contract. The fingerprint now binds `leader`, `second`, and outlet-retention roles in selection order; source and final browser regressions require reversed assignments to produce different IDs. | Preserve ordered role identity unless the product removes role-specific meaning from the two selections; any future fingerprint change must retain threshold, staffing, split, and abstention bindings. |
| D66 | Keep the a570 two-role, two-area loop as the final tactical product because it names both teams | rejected | Portugal's four-way endpoint profile is a bounded forecast, but the displayed probabilities do not use Uruguay's defensive exposure. Compressing the team profile to the two selected areas gives exactly the same aggregate holdout coverage as the tournament top two (`65/84`, `59/76`). The role tokens, threshold, and outlet tradeoff are not observed tactical actions. Independent football and UX red teams both returned `REJECT`. | Do not publish video `20fdc43d…` or submit a570 as the final product. Reopen only if the user's visible decision is directly powered by admitted team-specific evidence. |
| D67 | Replace defensive-role placement with a two-record, three-situation rehearsal allocation | accepted | The raw event transform separates Portugal attack `7/5/2`, Uruguay defensive situations `2/2/1` plus one unclassified endpoint, and the held-out Uruguay–Portugal `5/2/3`. The official rehearsal converts only the visible Portugal ratio to `5/4/1`, locks before reveal, then shows raw differences and saves the next meeting's decision. Players rights/semantic evidence, deterministic reproduction, Korean copy, football review, mobile comprehension, result accessibility, and the four-browser static release now pass. | Preserve separate team records, event-language boundaries, no score or recommendation, the mobile first-action contract, and fail-closed accepted-source build gate. Reopen only with stronger rights-cleared tracking evidence and a predeclared falsifiable replacement. |
| D68 | Admit Players v3 profile fields broadly because the source is CC BY 4.0 | rejected | CC BY requires attribution and change notice but does not grant publicity, privacy, personality, trademark, or endorsement rights. Most profile fields are unnecessary for the rehearsal decision. | The only candidate public fields are `player_id` and a one-pass Unicode-decoded, NFC-normalized `display_name` from `shortName`. Exclude names composed from other fields, birth/passport data, height, weight, foot, role, current-team IDs, photos, flags, crests, rankings, and recommendation claims. |
| D69 | Describe the player on the first follow-up event as the receiver, first-contact player, or duel winner | rejected | The Events item defines `playerId` as the player associated with a recorded event. It does not guarantee physical first contact, reception, or duel victory. | Use only `first recorded follow-up actor` and `first recorded defending actor`, bind source event ID/type/subtype/offset and join status, and fail closed on missing or duplicate joins. |
| D70 | Quarantine Rui Patrício's held-out Air-duel actor link as an impossible goalkeeper anomaly | rejected | The raw record is the final Portugal corner at 94:47.663, followed 2.582 seconds later by Portugal's Air duel at 94:50.245. Independent review found that RTVE's report and live account explicitly record Rui Patrício going up for the final corner and heading the ball. A goalkeeper-role heuristic would therefore erase a valid exceptional event. | Keep the source `shortName` only as the player associated with the recorded event. Bind the reviewed outlier in the Players semantic audit; do not expose the private role field or infer reception, first contact, duel victory, or performance. |
| D71 | Lead mobile users through the full method and planning correction before the first allocation control | rejected | At 390×844 the first `+` control initially appeared after more than three screens. The revised order puts `14/14`, `5/6`, the goal, and allocation cards directly after the hero; method and correction details move under progressive disclosure, and the pre-reveal commit bar is sticky. Independent mobile review and the new viewport contract return PASS. | Preserve the first-action y-position contract, 44px controls, no horizontal overflow, visible small-sample boundary, and closed method details at narrow widths. |
| D72 | Use the visible group-stage `7/5/2` counts as a hidden recommendation for the official demo | rejected | A visually unexplained `5/4/1` could look like a model optimum. The canonical demo now states that `5/4/1` is only the largest-remainder conversion of the visible counts to ten rehearsal repetitions and is not an optimal allocation. | Narrate the conversion and non-recommendation before lock; compare it with held-out `5/2/3` only as raw differences and a next-meeting question. |
| D73 | Keep the validated team-situation experience as an isolated prototype while deploying a570 | rejected | The canonical release builder now binds `prototypes/opponent-scouting`, requires accepted and rights-cleared Events/Matches/Players, and refuses a non-PASS situation artifact. The built app passes the keyless loop, immutable memo, invalid-data fail-closed, axe, and responsive contracts across Chromium, Firefox, WebKit, and mobile (`12/12`). | All release, gallery, demo, public-deployment, and submission evidence must now use Corner Prep Lab bytes. Old a570 screenshots, video, and story artifacts are superseded and cannot be uploaded. |
| D74 | Keep the `5/4/1` ten-repetition allocation because it is deterministic and technically validated | rejected | The user correctly identified that the interaction still reduced sparse team histories to category frequencies. Moving ten tokens did not expose what Portugal repeatedly did, whether Uruguay had faced the same event chain, or which unselected pattern later produced counterevidence. Technical validity did not make the decision tactically meaningful. | The video `8f9b759e…99ba0`, its gallery, and its release story are superseded and must not be uploaded or submitted. |
| D75 | Use five `restart family × first recorded follow-up team` signatures and let the manager lock two matchup questions | accepted | Raw reproduction gives exact pre-match Portugal/Uruguay counts `7/2`, `1/2`, `4/0`, `1/0`, and `1/1`. The hidden match gives `5`, `2`, `0`, `0`, and `3`; the unselected other-non-short/defending-first signature contains two ten-second shots and deterministic first receipt `261095314`. The visible choice is now driven by both teams' admitted event chains. | Preserve separate team ledgers, exactly two questions, lock-before-reveal, deterministic unselected counterevidence, and source receipts. Reopen if Korean five-second comprehension or the full release matrix fails. |
| D76 | Describe the five-signature board as a complete team tactical analysis or use a zero count as a defensive weakness | rejected | The source records events, event-associated players, and locations at discrete moments; it does not contain lineups in phase, marking assignments, reach, physical first contact, rehearsed intent, or a causal counterfactual. Uruguay has only five classifiable pre-match defensive exposures. | Say `사전 관찰 공백` for zero and `팀별 이벤트 연쇄 대조` for the product. Full weakness or optimal-tactic claims require rights-cleared tracking/formation evidence and a predeclared validation gate. |
| D77 | Promote the partially pooled Portugal forecast plus source-scene review board as Corner Scout Lab | accepted | Portugal's 14 classifiable group-stage corners receive `46.7%` evidence weight and the 397-corner tournament profile receives `53.3%` prior weight. The frozen team-conditioned model lowers log loss by `4.59%` on 160 unseen knockout corners, with a match-cluster 95% mean log-score-gain interval of `[0.0040, 0.1071]`. The first screen now distinguishes this validated team forecast from the separately observed Uruguay defensive scenes, and the two canonical questions each recur in all three Portugal group matches. | Preserve the full four-way profile, sample and shrinkage disclosure, frozen 160-corner audit, match recurrence, exact source receipts, and manager-owned review decision. Do not turn the Portugal delivery forecast into a defensive position, optimal tactic, or correctness score. |
| D78 | Pool five Uruguay defensive exposures into the forecast or call the observed scenes a tactical weakness | rejected | The predeclared two-team challenger improves mean log loss by `1.01%`, but `P(gain > 0)=0.9226` misses the `0.975` promotion gate and its match-cluster interval crosses zero. The source also lacks continuous tracking, formation, marking, rehearsed intent, and causal counterfactuals. | Keep Uruguay out of the displayed forecast. Use its sparse records only to locate source scenes and state `관찰 공백` where appropriate; reopen a weakness or matchup-model claim only with stronger rights-cleared data and a frozen challenger that passes every uncertainty gate. |
| D79 | Lead the final product with the partial-pooling forecast because it is statistically validated | rejected | Owner and independent UX review both read the first screen and video as a small-sample location-statistics dashboard. `14/397`, `47/53`, `4.59%`, and `9/10` displaced the player-event evidence and manager decision even though the forecast does not use Uruguay's defensive record. | Keep the forecast under `표본 보정과 검증 근거`. The first screen, gallery, and first 15 seconds of the demo must show observed Portugal player-event connections, thin Uruguay support, and the manager's scene-review choice. |
| D80 | Treat a shared broad signature as the same tactical scene across both teams | rejected | The five signatures combine only situation family and first-recorded team role. Within `short + attacking first`, Portugal contains crosses, simple passes, and a ground duel, while Uruguay's two receipts are both simple passes. No tracking, marking, or routine intent is present. | Show a Uruguay receipt as `직접 비교` only when situation family, first-recorded team role, and first follow-up event/sub-event match a Portugal receipt. Label the remaining same-family receipts `참고 장면`, retain corner event IDs, and never promote either class to a weakness or tactical response. |
| D81 | Promote a player-first, source-linked scene index as the corrected Corner Scout Lab interaction | revise | The committed source contains one repeated same-receipt connection across at least two matches: corner taker Ricardo Quaresma and first recorded follow-up actor Raphaël Guerreiro, three short-area receipts in two group matches. It does not establish a receiver, first contact, pass target, possession continuation, or rehearsed routine. The source also supports event-subtype comparison, match concentration, held-out receipts, and deterministic counterevidence without adding a causal claim. Targeted v12 calculation, story, copy, and claim-boundary gates pass; public release, full browser, gallery, and video gates remain open. | Accept only after the repeated connection and three-axis support are committed in the derivative, fail-closed in the browser, and bound through the complete release/video identity chain. |
