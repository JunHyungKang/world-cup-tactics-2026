# Corner Policy Lab canonical conversion

Status: `APPROVED — EXECUTE AFTER P0 FREEZE`

## Decision basis

Corner Policy Lab v5 scored `98/100` against Corner War Room `97/100` in an
exact-artifact same-reviewer comparison. The promoted local bundle is:

- release manifest `7c909cb523ecda75d23558f97910dc12d042ebabf029a371530ef4c52b8c2523`;
- planning capture manifest `62103aec67469f90853dfe33be68d677405691007eb4dab6369066f06922b696`;
- narrated rehearsal `506bfa08accfeab0911d195f1d35234d7203af2ec67c569b7ce9855807ded820`;
- eight-page plan `4b35fd07b5d70dc0b921c2bf892f896db27b59d34b408a1a867e91831df84524`.

Product selection is `PASS`. The empirical campaign remains `REVISE`, the
causal recommendation remains `REJECT`, and human evidence remains
`unavailable/no-claim`.

## Reversible conversion order

1. **Preserve the runner-up.** Require the 18-artifact manifest at
   `docs/archive/corner-war-room-2026-07-19/archive-manifest.json` to pass before
   replacing any canonical file.
2. **Make the root release Policy Lab.** Point default `dev`, `build`, and
   `preview` to the keyless Policy Lab static release and emit deployable files
   under `dist/`. Keep the CWR React/domain sources and tests as archived
   runner-up evidence, not as the root entrypoint.
3. **Rebind product identity.** Update `docs/product-selection.json`,
   `public/data/product-binding.json`, and `docs/data-scope-resolution.json` in
   that order. The product-selection status may be `PASS`; the data report must
   still reject causal policy estimation.
4. **Replace canonical product documents.** Rebind `docs/product-thesis.md`,
   `docs/first-place-goal.md`, the interaction contract, judging map, README,
   official state, and session handoff to Policy Lab. Update harness assertions
   rather than adding override flags.
5. **Promote the plan.** Make the Policy outline, five screenshots, renderer,
   eight-page PDF, visual review, and owner handoff the canonical planning
   pipeline. Re-run independent 8/8 visual QA; the CWR review cannot be reused.
6. **Promote story and gallery.** Rebind `docs/submission-story.json`, gallery,
   storyboard, Korean narration, captions, rehearsal, and their validators to
   the `48 → 8 → 8` one-lock story.
7. **Replace the final browser contract.** Port Policy Lab's full commit,
   secrecy, same-fingerprint, invalid-report, abstention, keyboard, mobile, and
   accessibility behavior into canonical `BG-01–15` tests across Chromium,
   Firefox, WebKit, and mobile.
8. **Freeze only from a clean commit.** Run the exact build and preflight, create
   the public GitHub repository and HTTPS deployment, run deployed `BG-12` and
   the full final suite, then record the final public URL and commit.
9. **Capture external evidence last.** Re-record from the stamped public URL,
   upload YouTube, complete the DAKER fields, and store the public URL, video URL,
   commit, and DAKER receipts together.

## Stop conditions

- Stop if root causal status changes from `REJECT` or the empirical campaign is
  described as an optimal, predictive, or defensive-effect policy.
- Stop if the final eight matches are revealed before the single policy lock, or
  if the two audits use different fingerprints.
- Stop if any public build needs a judge API key, login, payment, or network
  inference call.
- Stop if CWR archive verification, Korean copy audit, four-environment release
  tests, plan SHA binding, or final submission preflight fails.
- Do not claim public release, YouTube completion, DAKER submission, or human
  preference until literal external receipts exist.

## Current scheduling boundary

The competition board keeps this project in a bounded P1 lane until the P0 media
statistics submission is frozen. The next safe action after that boundary is
step 2, not an external upload.
