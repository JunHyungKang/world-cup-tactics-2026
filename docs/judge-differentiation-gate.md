# Judge Differentiation Product Gate

Verified: `2026-07-19T23:59:00+09:00`

Status: `PASS — Corner Policy Lab technical product proof passed; public release evidence pending; human outcomes unavailable/no claim`

## One-sentence manager fantasy

The judge chooses two corner-delivery areas, locks one policy before seeing the
knockout matches, and watches the historical record try to disprove it twice.

## Why this is not another RAG dashboard

The product never retrieves one similar scene and asks whether the answer looks
right. It creates a falsifiable campaign:

1. inspect only the 48 group-stage matches;
2. choose two of four delivery areas or abstain;
3. lock one immutable policy fingerprint;
4. reveal the eight round-of-16 matches and a deterministic representative
   contradiction;
5. expose the same unchanged policy to eight quarter-final-and-later matches
   that were still sealed.

The ontology is a safety mechanism, not a recommendation engine. It joins
`MatchContext`, `ScoutingPolicy`, `CornerRestart`, `DeliveryAction`,
`ObservedEvent`, `OutcomeProxy`, and `Source`, while explicitly forbidding
`WOULD_PREVENT`, `OPTIMAL_POLICY`, and `DEFENSIVE_DUTY_CAUSED`.

## Exact current proof

| Official criterion | Current evidence | Remaining gate |
|---|---|---|
| Originality 30 | fixed 48–8–8 campaign, one-lock/two-holdout falsification, ontology guardrails | preserve the loop in public release |
| Manager experience 25 | five activations, two scarce priorities, same-policy receipts, abstention and detailed revision path | public-URL browser proof |
| Completeness 25 | 603 corners, 557 classified, 46 visible missing endpoints; 7/7 data/release and 12/12 static browser checks | deployed BG-12 and final artifact review |
| Planning/implementation consistency 20 | canonical PDF, captures, release, story, gallery, narration, captions, and video are SHA-bound | bind clean commit and final URLs |

The first-round funnel remains submitter `60%`, participant `20%`, and public
`20%`. Human preference, comprehension, usability, and memorability are
unavailable and not claimed.

## Claim boundary

The product reports delivery-location overlap and representative observed
contradictions. It does not estimate defensive success, shot prevention, player
reach, xG change, win probability, an optimal policy, or a changed match result.
The product selection is `PASS`; causal recommendation is `REJECT`; the
empirical campaign is `REVISE`.

## Verdict

`PASS` for the selected product. The exact-artifact comparison scored Corner
Policy Lab `98/100` and the archived Corner War Room `97/100`. Promotion remains
conditional on an exact final PDF review, public GitHub and deployment, deployed
browser evidence, YouTube, and DAKER receipts. No local or agent-generated
artifact is human evidence or an external submission confirmation.
